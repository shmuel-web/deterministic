/** validate — re-score a diff after the agent, check completion. Lane 3. */
export async function validate(): Promise<void> {
  throw new Error("validate is not implemented yet — Lane 3 (the loop closer).");
}
