/** analyze-ticket — resolve a ticket's blast radius + score it. Lane 2. */
export async function analyzeTicket(ticketPath?: string): Promise<void> {
  if (!ticketPath) throw new Error("usage: deterministic analyze-ticket <ticket.md>");
  throw new Error("analyze-ticket is not implemented yet — Lane 2 (blast-radius resolver).");
}
